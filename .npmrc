# pnpm configuration for React Native compatibility
# shamefully-hoist is REQUIRED for React Native to work properly with pnpm
# It creates a flat node_modules structure similar to npm/yarn
shamefully-hoist=true

# Ensure all dependencies are accessible from the workspace root
node-linker=hoisted
